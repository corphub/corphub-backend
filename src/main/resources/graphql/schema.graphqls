type Query {
    # The API Version
    version: String!
}

type Category {
    id: Int!
    name: String!
}

extend type Query {
    getCategoryById(id: Int!) : Category
    getAllCategories : [Category]
}
